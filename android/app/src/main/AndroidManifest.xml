<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="xafit"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Flutter embedding -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!--
            Registro del widget de pantalla de inicio de XaFit.

            El sistema Android llama a XafitWidgetProvider cuando:
            - El usuario añade el widget al launcher por primera vez
            - Pasan los 30 min del updatePeriodMillis
            - La app llama a HomeWidget.updateWidget() desde Dart

            android:exported="true" es obligatorio para que el launcher
            pueda enviar el broadcast APPWIDGET_UPDATE al receiver.
        -->
        <receiver
            android:name=".XafitWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <!-- Apunta al xml con los metadatos del widget (tamaño, layout, etc.) -->
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/xafit_widget_info" />
        </receiver>

    </application>

    <!-- Necesario para que Flutter pueda procesar texto seleccionado -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
